#pragma once
#include <QWidget>

namespace odv {

// The theme editor (the PHP "theme engine": editable colour fields, live
// preview, save). STUB — replace with the real editor.
class ThemeDialog {
public:
    static void open(QWidget* parent);
};

}  // namespace odv
