#pragma once
// The build can override this (CI injects the auto-incremented release version via
// -DODV_VERSION=…); otherwise this baked-in value is used.
#ifndef ODV_VERSION
#define ODV_VERSION "0.2.0"
#endif
